[package]
name = "vptr"
version = "0.1.0"
edition = "2021"

[dependencies]
reqwest = { version = "0.12", features = ["blocking", "json"] }
serde_json = "1.0"
chrono = "0.4.42"
toml = "0.9.8"
serde = { version = "1.0", features = ["derive"] }